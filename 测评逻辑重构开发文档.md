# 测评逻辑重构开发文档

> 架构：五层串行流水线 | 目标：消除批量判题不稳定、错因同质化、计划空洞问题

---

## 一、整体数据流架构

```
用户提交三天答题数据
        │
        ▼
┌─────────────────────────────────────────┐
│  Layer 1: 题库标准化预处理（地基）       │
│  · 127题元数据已入库                     │
│  · 每题含: error_label_pool             │
│           radar_dimensions (维度+权重)   │
│           scoring_rubric (踩分点)        │
│           grading_mode (auto | llm)     │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Layer 2: 单题结构化阅卷（LLM调用层）    │
│  · 客观题(choice/fill) → 程序判分       │
│  · 主观题(step) → LLM一题一调用         │
│  · 输出: 单题阅卷JSON                   │
│  · 约束: 只从error_label_pool选错因      │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Layer 3: 程序聚合统计（纯代码, 禁LLM）  │
│  · 雷达图分值计算(维度权重加权)          │
│  · 总分 & 达标判定                       │
│  · 错题频次统计(知识点+错误标签)         │
│  · MBTI式维度置信度过滤                 │
│  · 输出: 结构化学情汇总表               │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Layer 4: 报告素材组装                   │
│  · 聚合: 单题阅卷JSON + 学情汇总表      │
│  · 输出: 纯净结构化素材包               │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Layer 5: 最终LLM文案生成（第二次调用）  │
│  · 输入: 仅学情汇总统计结果             │
│  · 输出: 个性化错因归纳 + 四周计划      │
│  · 约束: 对应薄弱知识点, 杜绝泛化话术   │
└─────────────────────────────────────────┘
```

### 核心设计原则
1. **分片调用**：主观题一题一调用，规避超长上下文溢出
2. **计算分离**：所有数值计算由程序完成，LLM仅做文本解析与生成
3. **标签约束**：错因只能从预设标签池选择，禁止AI自由编造
4. **素材净化**：传给报告生成LLM的是汇总统计而非原始答题记录

---

## 二、各层级接口规范

### Layer 1: 题库标准化元数据

每道题在数据库中需包含以下新增字段：

```json
{
  "question_id": "S1-D1-01",
  "grade": "初一",
  "day_tag": 1,
  "seq_no": 1,
  "q_type": "choice",
  "grading_mode": "auto",

  "radar_dimensions": [
    {"dimension": "YS-08", "weight": 1.0}
  ],

  "error_label_pool": [
    {
      "code": "EC-K1",
      "label": "概念未建立",
      "description": "认为'记作几米'就是写路程数值，忽略相反意义的量中正负的方向规定"
    },
    {
      "code": "EC-C2",
      "label": "算术定势迁移",
      "description": "小学'求相差多少'的算术定势，把新情境套进旧算法"
    }
  ],

  "scoring_rubric": {
    "full_score": 2,
    "rubric_items": [
      {"description": "选对B得2分", "score": 2},
      {"description": "选错或不选得0分", "score": 0}
    ]
  },

  "knowledge_points": {
    "primary": {"code": "KP-01.1", "name": "正负数意义"},
    "secondary": null
  }
}
```

**字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| `grading_mode` | `auto` \| `llm` | choice/fill=auto, step=llm |
| `radar_dimensions` | Array | 从literacy_codes映射，weight默认1.0 |
| `error_label_pool` | Array | 从ec_mapping+干扰项设计提取，LLM只能从此池选 |
| `scoring_rubric` | Object | 踩分点详细描述 |
| `knowledge_points` | Object | 一级+二级知识点 |

### Layer 2: 单题阅卷接口

**输入（LLM调用参数）**：
```json
{
  "question_id": "S1-D2-15",
  "stem": "化简：2(3a-b)-3(a-2b)",
  "q_type": "step",
  "scoring_rubric": {
    "full_score": 3,
    "rubric_items": [
      {"seq": 1, "description": "去括号: 6a-2b-3a+6b", "score": 1},
      {"seq": 2, "description": "合并同类项: 3a+4b", "score": 2}
    ]
  },
  "error_label_pool": [
    {"code": "EC-K2", "label": "去括号法则未完整执行"},
    {"code": "EC-M1", "label": "系数合并计算错误"},
    {"code": "EC-M2", "label": "跳步导致中间结果丢失"}
  ],
  "student_answer": {
    "step1": "6a-2b-3a-6b",
    "step2": "3a-8b"
  }
}
```

**输出（LLM返回JSON）**：
```json
{
  "question_id": "S1-D2-15",
  "full_score": 3,
  "student_score": 0,
  "is_correct": false,
  "matched_error_labels": ["EC-K2"],
  "brief_error_analysis": "去括号时-3(a-2b)展开为-3a-6b，第二项未变号（应为+6b），属去括号法则半执行",
  "related_kp": "KP-04.3"
}
```

### Layer 3: 学情汇总表接口

**输入**：所有单题阅卷JSON数组

**输出（结构化学情汇总表）**：
```json
{
  "total_score": 68,
  "full_score": 110,
  "pass_threshold": 75,
  "is_passed": false,
  "grade_level": "待加强",

  "radar_chart": {
    "YS-01": {"score": 72, "level": "基本达标", "question_count": 12, "valid": true},
    "YS-02": {"score": 85, "level": "达标", "question_count": 18, "valid": true},
    "YS-03": {"score": 45, "level": "薄弱", "question_count": 8, "valid": true},
    "YS-05": {"score": 60, "level": "不牢", "question_count": 10, "valid": true},
    "YS-07": {"score": 0, "level": "无数据", "question_count": 0, "valid": false},
    "YS-08": {"score": 0, "level": "无数据", "question_count": 0, "valid": false}
  },

  "error_frequency_by_kp": [
    {"kp_code": "KP-01.3", "kp_name": "绝对值与相反数", "error_count": 3, "total_count": 4, "error_rate": 0.75},
    {"kp_code": "KP-04.3", "kp_name": "去括号法则", "error_count": 2, "total_count": 3, "error_rate": 0.67}
  ],

  "error_frequency_by_label": [
    {"code": "EC-K2", "label": "符号法则误用", "count": 5, "percentage": 0.31},
    {"code": "EC-C1", "label": "审题不完整", "count": 3, "percentage": 0.19},
    {"code": "EC-M1", "label": "计算错误", "count": 2, "percentage": 0.13}
  ],

  "weak_knowledge_points": [
    {"kp_code": "KP-01.3", "name": "绝对值与相反数", "error_rate": 0.75, "severity": "高"},
    {"kp_code": "KP-04.3", "name": "去括号法则", "error_rate": 0.67, "severity": "高"}
  ],

  "genuine_response_stats": {
    "total_questions": 49,
    "genuine_answers": 42,
    "genuine_ratio": 0.857,
    "abandoned_count": 2,
    "invalid_input_count": 5
  }
}
```

### Layer 4: 报告素材包接口

```json
{
  "summary_table": { /* Layer 3 输出 */ },
  "per_question_results": [ /* Layer 2 所有单题JSON */ ],
  "report_meta": {
    "student_name": "木木",
    "grade": "初一",
    "test_date": "2026-08-15",
    "sku_code": "S1"
  }
}
```

### Layer 5: LLM文案生成接口

**输入（仅汇总统计结果）**：
```json
{
  "grade": "初一",
  "total_score": 68,
  "full_score": 110,
  "grade_level": "待加强",
  "weak_knowledge_points": [...],
  "error_frequency_by_label": [...],
  "error_frequency_by_kp": [...],
  "radar_chart": {...}
}
```

**输出**：
```json
{
  "error_analysis": "该生在绝对值与相反数（KP-01.3）模块表现薄弱，错误率75%，核心问题为去括号时符号法则半执行...",
  "four_week_plan": [
    {
      "week": 1,
      "focus_kp": "KP-01.3 绝对值与相反数",
      "exercises": [
        {"content": "符号分级读题练习：-|-5|、-(-3)、|-3|三式对比", "reason": "该生在D1-03选A，漏读最外层负号"},
        {"content": "三步写题法练习：由|x|=7写出两种可能→条件筛选→求相反数", "reason": "该生在D1-04填±7，不会用条件取舍"}
      ]
    },
    {
      "week": 2,
      "focus_kp": "KP-04.3 去括号法则",
      "exercises": [
        {"content": "含负系数分配练习：-3(a-2b)展开为-3a+6b", "reason": "该生在D2-15去括号第二项未变号"}
      ]
    }
  ]
}
```

---

## 三、分层Prompt模板

### 3.1 单题阅卷Prompt（Layer 2 - 主观大题专用）

```
你是一名严谨的初中数学阅卷教师。请对学生的答题进行结构化阅卷。

## 题目信息
- 题目ID: {question_id}
- 题干: {stem}
- 满分: {full_score}

## 评分踩分点
{scoring_rubric_json}

## 预设错误标签池（你只能从此池中选择错误标签，禁止编造标签池外的错因）
{error_label_pool_json}

## 学生作答
{student_answer_json}

## 阅卷要求
1. 严格按照踩分点打分，每步独立判分
2. 如果学生答错，必须从上方【预设错误标签池】中选择匹配的标签
3. 简短分析错因（一句话，指出具体错在哪里）
4. 只输出JSON，禁止附带任何解释文字

## 输出格式（严格按此JSON结构）
{
  "question_id": "题目ID",
  "full_score": 满分,
  "student_score": 学生得分,
  "is_correct": true或false,
  "matched_error_labels": ["标签code"],
  "brief_error_analysis": "一句话错因",
  "related_kp": "知识点code"
}
```

### 3.2 报告生成Prompt（Layer 5 - 最终文案）

```
你是一名资深数学教育诊断分析师。请基于以下学情统计结果，生成个性化诊断报告文案。

## 学生学情汇总数据
{summary_table_json}

## 报告生成要求
1. 综合错因归纳：结合错误频次最高的标签和薄弱知识点，分析该生的核心问题模式
2. 四周提升计划：
   - 每周聚焦1-2个薄弱知识点（从weak_knowledge_points中按严重程度排序）
   - 每条训练内容必须写明：训练知识点 + 训练理由（对应学生同类错误的具体表现）
   - 杜绝"多加练习、夯实基础"等泛化话术
3. 只输出JSON，禁止附带多余解释

## 输出格式
{
  "error_analysis": "200字以内的个性化错因归纳",
  "four_week_plan": [
    {
      "week": 1,
      "focus_kp": "知识点名称",
      "exercises": [
        {"content": "具体训练内容", "reason": "对应学生某题的具体错误表现"}
      ]
    }
  ]
}
```

---

## 四、客观题/主观题分支处理逻辑

```
学生提交答案
    │
    ├── q_type === "choice"?
    │   └── YES → 程序判分: gradeChoice(studentKey, correctKey)
    │             → 错因: 从options[studentKey].ec_code获取
    │             → 输出单题阅卷JSON（不调用LLM）
    │
    ├── q_type === "fill"?
    │   └── YES → 程序判分: normalizeFill(raw, answer_spec)
    │             → 错因: 从ec_mapping按答案特征匹配
    │             → 输出单题阅卷JSON（不调用LLM）
    │
    └── q_type === "step"?
        └── YES → LLM判分: 调用单题阅卷Prompt
                  → 一题一调用，传入题干+踩分点+标签池+学生作答
                  → 校验LLM返回JSON格式
                  → 输出单题阅卷JSON
```

### 客观题自动判分流程
1. 选择题：精确匹配选项key → 正确=满分，错误=0分 → 从options[].ec_code获取错因
2. 填空题：normalize → 按answer_spec判分 → 从ec_mapping按规则匹配错因
3. 无需LLM调用，纯代码完成

### 主观题LLM判分流程
1. 组装Prompt：题干 + 踩分点 + 错误标签池 + 学生原始作答文本
2. 调用LLM（O2模型）
3. 解析返回JSON，校验字段完整性
4. 验证matched_error_labels是否都在标签池内（不在则标记为"unmatched"）
5. 输出标准化单题阅卷JSON

---

## 五、MBTI式维度置信度过滤算法

### 雷达图维度计分规则

```
对于每个素养维度 D（如YS-01）:
  1. 收集所有关联该维度的题目集合 Q_D
  2. 过滤：只保留真实有效作答（isGenuineResponse=true）的题目
  3. 计算维度得分:
     - 有效题数 ≥ 5: score = (答对题数 / 有效题数) × 100
     - 有效题数 3-4: score = (答对 + 1.5) / (有效 + 3) × 100  （贝叶斯平滑）
     - 有效题数 = 2: score = (答对 + 2) / (有效 + 4) × 100
     - 有效题数 < 2: score = null, level = "数据不足"（该维度不显示）
  4. 维度等级:
     - score ≥ 80: "达标"
     - score ≥ 50: "不牢"
     - score < 50: "薄弱"
```

### 零作答场景处理
```
if (genuine_ratio < 0.25 || genuine_count < 4):
  radar_chart = {} (空)
  module_mastery = {} (空)
  error_analysis = "本次诊断为无效答卷，有效作答率不足25%，无法生成能力评估"
  four_week_plan = []
  → 报告页显示空白状态说明
```

---

## 六、Demo测试方案与异常处理

### 测试场景

| 场景 | 操作 | 预期结果 |
|------|------|---------|
| 正常作答 | 注册初一→认真完成3天→查看报告 | 雷达图正常+错因分析有针对性+四周计划对应薄弱点 |
| 零作答 | 注册初一→全部跳过→查看报告 | 雷达图空白+无效答卷警告 |
| 部分作答 | 注册初二→只做Day1→查看报告 | 雷达图部分维度显示+提示数据不完整 |
| 主观题阅卷 | 初二Day2分步题→故意写错→查看报告 | LLM正确识别错因标签+分析具体错误 |

### 异常处理规则

| 异常情况 | 处理方式 |
|---------|---------|
| LLM调用超时 | 重试1次，仍失败则降级为程序判分（按步骤答案精确匹配） |
| LLM返回格式错误 | JSON解析失败→降级为程序判分 |
| LLM返回标签池外的错因 | 标记为"unmatched"，记录但不展示 |
| 客观题答案为空 | 判0分，标记behavior_tag="abandoned" |
| 数据库写入失败 | 返回500错误，前端提示"报告生成失败，请重新提交" |

---

## 七、代码模块划分

### 代码实现模块（纯代码，不调用LLM）
| 文件 | 职责 |
|------|------|
| `domain/engine/grading.ts` | 客观题判分（已有，保留） |
| `domain/engine/aggregator.ts` | **新增** Layer 3 聚合统计引擎 |
| `domain/engine/reportAssembler.ts` | **新增** Layer 4 素材组装 |
| `domain/engine/mastery.ts` | 掌握度计算（已有，增强） |
| `domain/engine/credibility.ts` | 置信度校验（已有，保留） |

### LLM调用模块
| 文件 | 职责 |
|------|------|
| `domain/engine/llmGrader.ts` | **新增** Layer 2 单题LLM阅卷 |
| `domain/engine/reportGenerator.ts` | **新增** Layer 5 LLM文案生成 |
| `domain/engine/promptTemplates.ts` | **新增** Prompt模板管理 |

### API路由
| 文件 | 职责 |
|------|------|
| `app/api/session/submit/route.ts` | 修改：接入五层流水线 |
| `app/api/report/get/route.ts` | 修改：返回新格式报告 |
